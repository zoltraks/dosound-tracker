Modify the way steps are colored in tracks depending on whether the track-coloring mode is enabled (default) or disabled (rainbow toggled).
If the track-coloring mode is disabled, then instead of setting the background color for a step that contains an instrument with a defined color,
change the font color in that step so that, in the dark theme, the color is slightly lighter than the instrument’s defined color,
and in the light theme, slightly darker. And in this case don't modify background.
Keep background coloring without font-color adjustments in the default, initial coloring mode.

---

On instrument list current selected instrument has input field instead of span but its background differs when instrument has color assigned.
Make input field to have the same background color like span of "instrument-id" or span of "instrument-name".

---

Make "position-block" and "track-panel" to have the same border style like "envelope-panel", "playlist-panel" or "instrument-panel".

---

When changing track coloring with "rainbow" button I want transition effect for background color.
But disable background transition when current step is changed.

---

It seems that in track panel current highlighted step that is track-line current but not active seems to have additional padding 1px. Remove it.

---

But now the current and active step has wrong padding, it should be moved 1px right and 1px top

---

When enabling or disabling track colors and step background mode by clicking on "rainbow" button I would like to have transition effect so it smoothly changes colors but only while changing track coloring mode option

---

Make this transition 2x slower and make it work also for "track-a", "track-b", "track-c" background color as it now changes immediately after toggling track coloring mode

---

Still track background is changed immediately without transition

---

Instead of using "🎶" in div with CSS class "logo" use SVG icons:
 - src/assets/svg/logo-dark.svg for dark theme
 - src/assets/svg/logo-light.svg for light theme
When "information-panel" block has active CSS class, only visible border changes color and there is no additional border.
But when "instrument-panel" has active class enabled it has double border frame.
Make it to have only single border frame like "information-panel"

---

But when "instrument-panel" is currently focused it has additional frame.
Make it to have only single border frame like "information-panel" which when focused has only visible border.

---

Now make the same change for "playlist-panel" and "envelope-panel".

---

In mobile view command buttons are aranged in rows and three columns.
Make button in first column to be moved 1px left and button in third column to be moved 1px right.
Probably you need to change padding or margin.

---

Move third column another 1px right.

---

Remove this double border also from focused buttons in command panel

---

When button is focused it is visually 1px top from its original position. 
Make it to remain at its original position.

---

Make hover not to use translateY(-1px)

---

Remove this double border also from "track-panel"

---

When "information-panel" block has active CSS class, only visible border changes color and there is no additional border.
But when "track-panel" gains active class enabled it has double border frame.
Make it to have only single border frame like "information-panel"

---

The content of "instrument-item-container" element seems like having 3px too much height and in mobile view where the whole block has dynamic height I still can scroll 3px top or bottom. Leave scrollbar always visible.

---

When instrument-panel has no active style, then row with current selected instrument has nice border but it disapears when instrument-panel gains active. Fix that so border around selected row remains.

---

You may remove vertical scrollbar in mobile view in "instrument-item-container", it will not be used

---

While playing a song at the moment playlist line is changed it causes scrolling on mobile view but it should not change the viewport position.

---

During playback the selected step in track is changed to reflect current position being played.
I can see that the background color of the "note-text" or "volume-data" that is gaining or loosing "active" style is transitioning but it should not.

---

When moving mouse cursor over rows of instrument-item-container list the background color is changing but it should be mixed with the background color of the instrument that has color set.

---

The content of "playlist-line-container" element seems like having 3px too much height and in mobile view where the whole block has dynamic height I still can scroll 3px top or bottom.
You may remove vertical scrollbar in mobile view in "playlist-line-container", it will not be used.

---
