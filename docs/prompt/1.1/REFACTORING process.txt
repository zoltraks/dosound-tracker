Continue with:

Wire useInstrumentActions into App.tsx:
Add useInstrumentActions import.
Replace the inline instrument action handlers with a single useInstrumentActions call that returns:
 - handleRenameInstrument
 - handlePlayInstrument
 - handleCloneInstrument
 - handleDeleteInstrument
 - handleMoveInstrument
 - handleCancelInstrumentDelete
 - handleConfirmDeleteInstrumentAndNotes
 - handleConfirmDeleteInstrumentOnly
Keep non-action helpers in  App.tsx (e.g. handleInstrumentSelect, handleOpenInstrumentMidi, handleSaveInstrumentMidi, handleChangeBaseKey).

And then extract song I/O into src/utils/songIO.ts:

 - move the YAML-heavy logic from saveSong, loadSong, saveInstrument, loadInstrument into pure utility functions.
 - keep the React-specific state wiring (setCurrentSong, setCurrentInstrument, setIsSongDirty, DOM download triggers) inside 
useDataManagement , but make them thin wrappers around the new utilities.

And then proceed with step 2  (hooking useInstrumentActions into  App.tsx) and then step 3 (song I/O extraction)

---

Tidy any remaining hook dependency warnings in useDataManagement / useMidiActions in a minimal, non-behavior-changing way

---

Make refactorings across the plan for all phases. Use existing file naming conventions regardless the guidelines from regactoring proposal.

---

Focus on further reducing MidiModal.tsx size (moving YAML load/save into a tiny helper) while keeping behavior frozen

---

Add a small unit test file for midiConfig.ts to make the config format contract explicit, mirroring the style of your existing utils tests

---

Run "npm run lint" and fix lint problems regarding "Cannot access variable before it is declared"

---

Address some of the safer remaining hook dependency warnings in useMidiActions and useAppState in the same minimal, non-behavior-changing style

---
