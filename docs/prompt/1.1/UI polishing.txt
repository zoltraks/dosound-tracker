Make change in "Track" block so all the time the current selected step have different background so the current step is always visible no matter if "Track" block is focused or not.

---

Remove alert warning about "Changes you made may not be saved.", we don't need this confirmation.

---

Remove also Electron quit confirmation.

---

Delete "DELETE PATTERN" button as it's not wired.
Instead introduce "DELETE TRACK" which should clear pattern number in selected track in current selected playlist line (leave pattern in memory).

Change name "DEBUG" to "BUG".
Change name "NEW TRACK" to "ADD TRACK".
Change name "NEW INST" to "ADD INST".

Arrange buttons in three rows instead of currently used four.

Row 1: "PLAY SONG", "PLAY PATTERN", "STOP", "OPTIMIZE", "RENUMBER", "TRANSPOSE", "EXPORT", "MIDI", "BUG", "RESET"

Row 2: "NEW SONG", "LOAD SONG", "SAVE SONG", "ADD LINE", "CLONE LINE", "DUPLICATE LINE", "ADD TRACK", "COPY TRACK", "PASTE TRACK", "INSERT STEP", "DELETE STEP"

Row 3: "ADD INST", "LOAD INST", "SAVE INST", "PLAY INST", "CLONE INST", "DELETE INST", "DELETE TRACK", "DELETE LINE"

---

When clicking on any octave buttons automatically make the octave block focused.

---

Rearange buttons in command panel:

Row 1: "PLAY SONG", "PLAY PATTERN", "STOP", "MIDI", "NEW SONG", "LOAD SONG", "SAVE SONG", "OPTIMIZE", "RENUMBER", "TRANSPOSE", "BUG", "RESET"
Row 2: "ADD LINE", "CLONE LINE", "DUPLICATE LINE", "DELETE LINE", "ADD TRACK", "COPY TRACK", "PASTE TRACK", "DELETE TRACK", "INSERT STEP", "DELETE STEP"
Row 3: "ADD INST", "LOAD INST", "SAVE INST", "PLAY INST", "CLONE INST", "DELETE INST", "EXPORT"

---

Move "MIDI" button before "BUG".

---

In instrument MIDI settings ("Instrument MIDI Output") there are two blocks with input fields and numeric up and down buttons using class "info-input".
Extract CSS classes for elements of NumberSpinner. Currenlty a span containing input, input field and buttons contain lot of style definition that should be defined in CSS in separate class names.

In Transpose modal window change input type number for "Semitones:" into NumberSpinner with proper settings for that window (number of semitones -99 to +99).

---

In "Export" modal window move "DATA" button before "DUMP" button.

---

Add one key at the end of piano keyboard, so there should always be highest "C" button available.

---

After review I can see that adding one key at the end of piano keyboard is not looking good.
Make it configurable by constant parameter and by default fall back to previous state, where the highest "C" button was not available.

---

Change CSS class names for better readibility and include these changes in the code that uses that class.

"--debug-btn-bg" to "--debug-btn-bg-left"
"--debug-btn-bg-2" to "--debug-btn-bg-right"
"--debug-btn-active-bg" to "--debug-btn-active-bg-left"
"--debug-btn-active-bg-2" to "--debug-btn-active-bg-right"

---

Change CSS class names for better readibility and include these changes in the code that uses that class.

"--reset-btn-bg" to "--reset-btn-bg-left"
"--reset-btn-bg-2" to "--reset-btn-bg-right"

---

Based on how "RESET" button has CSS defined:


```
.command-btn.reset-btn {
  background: linear-gradient(135deg, var(--reset-btn-bg-left) 0%, var(--reset-btn-bg-right) 100%);
  border-color: var(--reset-btn-border);
}

.command-btn.reset-btn:hover {
  background: linear-gradient(135deg, var(--reset-btn-bg-right) 0%, var(--reset-btn-left) 100%);
  border-color: var(--reset-btn-border);
}
```

Introduce new CSS classes for buttons "LOAD SONG", "SAVE SONG", "NEW SONG" in similar way making CSS classes and use them.

Choosing colors for bg-left and bg-right mix default color from theme: 

 - button background "LOAD SONG" mix with cyan
 - button background "SAVE SONG" mix with green
 - button background "NEW SONG" mix with purple

---

Rename button titles "DUPLICATE LINE" to "CLONE LINE" and "CLONE LINE" to "DUPLICATE LINE".

From now on "duplicate" means just make the same entry duplicated and "clone" means deep copy, such as creating new playlist entries with new patterns copied from the original copy source.

Apply this rename also in the variables and function names.

---
