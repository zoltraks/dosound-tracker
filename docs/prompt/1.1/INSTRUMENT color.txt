In instrument list for each instrument add a small button just before "MIDI" to allow set the color for the instrument.
Instead using a text label for that button, use the icons from src/assets/svg/ where are color-mode icon files for dark and light theme.

When user clicks a color, show Instrument Color settings modal window with color picker to allow user to select the color.

Add "CANCEL" and "CLEAR" buttons on the left side of modal window and "OK" on the right side.

When a user clicks "CANCEL" no change should be made.

When a user clicks "CLEAR" then the empty color (no color setting) should be set.

When instrument has a color assigned then on instrument list the "instrument-item" block of that instrument should have the tone of the assigned color.
Mix that color according to current background color which varies in the different theme selections.

As a standard we are using three hex digits code for color but you should allow importing data where color is set in 6-hex digit format like "#e4f5d3" and translate it to the nearest 3-hex digit format.

Include this color setting when saving or loading instrument data in YAML format.

```
instrument:
  name: Double Pim
  type: dosound
  version: 1
  volume: [13, 12, 0, 0, 12, 11, 0]
  arpeggio: [12, 0, 0, 0, 12, 0]
```

If instrument has assigned color then write additional "color" field before "mode" and "volume" fields.
If color is not set, don't write field "color" in YAML.

```
instrument:
  name: Double Pim
  type: dosound
  version: 1
  color: "#ddd"
  volume: [13, 12, 0, 0, 12, 11, 0]
  arpeggio: [12, 0, 0, 0, 12, 0]
```

Also include it when saving and loading song in YAML format similar way.

---

Make the elements in instrument list that are using "current" or "focused" style also use color mix for instruments that have color set like it is used with non current and not focused with "instrument-item-colored" style

---

Use also instrument color for each step in track content that has an entry with instrument number that has specified color element.
Remember that this color should be also included when step is current one. 

---

Tracks are using background-color and background linear-gradient defined in CSS styles: "track-a", "track-b", "track-c".

Add a small button to toggle background color for tracks.
Place it in position-block in position-header.
By default do nothing. 
When toggled, then disable background coloring in tracks.
Remember state of this button until "RESET" is made.
Instead using a text label, use icon from svg.
There are two versions for light and dark theme style: src/assets/svg/rainbow-mode-light.svg and src/assets/svg/rainbow-mode-dark.svg.

---

Don't focus rainbow mode button in position-header after click.

---

Use additional icons:
 - src/assets/svg/rainbow-mode-light-colorless.svg for light theme
 - src/assets/svg/rainbow-mode-dark-colorless.svg for dark theme
when the state of rainbow mode button was changes so coloring track is enabled.
If coloring track is disabled (toggled) then use the original ones:
 - src/assets/svg/rainbow-mode-light.svg for light theme
 - src/assets/svg/rainbow-mode-dark.svg for dark theme

---
